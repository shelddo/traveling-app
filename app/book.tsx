// criar tela de agendar viagem pra determinada cidade

import { View, Text } from "react-native";

export default function Book() {
    return (
        <View>
            <Text>Tela de agendar</Text>
        </View>
    );
}